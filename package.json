{
  "name": "guzo-fit",
  "version": "1.1.0",
  "private": true,
  "description": "Guzo Fit — strength and health training that re-plans itself around a life that keeps changing shape. Single-file offline PWA, no backend.",
  "type": "module",
  "scripts": {
    "build": "./build.sh",
    "dupes": "node dupes.mjs",
    "sw": "node sw.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
